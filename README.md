In progress*

The code is currently specific to my rig which uses a nema 17 stepper motor connected to a drv8825 driver for rotating the lidar on a seperate axis to its base rotation, and uses a arduino nano esp32 with a as5600 encoder for tracking the angle of rotation.
